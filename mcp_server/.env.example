# Only needed when running the MCP server standalone for manual tool testing,
# or when running its pytest suite. The backend passes its own environment
# through when it spawns the server as a subprocess.
DATABASE_URL=postgresql://shopkeeper:change_me@localhost:5432/shopkeeper_db
DEFAULT_SHOP_ID=1
