# Only needed when running the backend locally (outside Docker).
# Copy to .env: cp .env.example .env
DATABASE_URL=postgresql+asyncpg://shopkeeper:change_me@localhost:5432/shopkeeper_db
OPENAI_API_KEY=sk-your-openai-key-here
AGENT_MODEL=gpt-4o
CORS_ORIGINS=http://localhost:5173
DEFAULT_SHOP_ID=1
REMINDER_LOOKAHEAD_DAYS=3
REMINDER_CHECK_INTERVAL_HOURS=24
